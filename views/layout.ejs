<%- include ./layout/header.ejs %>


<div class="profile-wrap">
    <div class="profile">
        <% if (user && user.id) { %>
            <div class="user-name">안녕하세요! <%=user.id%> 님</div>
            <div class="half">
                <div>원래는 팔로잉 있던 자리</div>
            </div>
            <input type="hidden" id="my-id" value="<%=user.id%>"/>
            <a class="btn" id="logout" href="/auth/logout">로그아웃</a>
        <%} else { %>
        <form id="login-form" action="/auth/login" method="post">
            <div class="input-group">
                <label for="id">아이디</label>
                <input id="id" name="id" required="required" autofocus="autofocus" />
            </div>
            <div class="input-group">
                <label for="password">비밀번호</label>
                <input id="password" type="password" name="password" required="required" />
            </div>
            <%if (loginError) { %>
                <div class="error-message"> <%=loginError%> </div>
            <% } %>
            <a class="btn" id="join" href="/join">회원가입</a>
            <button class="btn" id="login" type="submit">로그인</button>
        </form>
        <% } %>

    </div>
</div>




